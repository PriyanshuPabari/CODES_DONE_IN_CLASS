#include <stdio.h>
int main()
{
    int i=1, number;
    printf("enter any number:");
    scanf("%d", number);

    for (i=1; i<=10; ++i)
    {
        printf("%d*%d=%d\n", i, number, i*number);
    }
    return 0;
}